{
  "name": "cashflow-analysis",
  "private": true,
  "scripts": {
    "start": "concurrently -k -n api,web -c blue,green \"npm run start:api\" \"npm run start:web\"",
    "start:api": "cd backend && .venv/bin/python main.py",
    "start:web": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
